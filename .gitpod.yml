image: gitpod/workspace-full-vnc
ports:
  - port: 9876
    visibility: private
    protocol: http

tasks:
  - name: Whaleon
    init: |
      sudo apt-get update && sudo apt-get install -y supervisor
